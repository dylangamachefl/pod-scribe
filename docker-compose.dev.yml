# docker-compose.dev.yml
# Development override for docker-compose.yml
# Use with: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up

version: '3.8'

services:
  # Frontend - Enable live-reload for development
  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules
