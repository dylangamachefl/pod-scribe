# ML/Audio Processing Dependencies
# These are installed AFTER PyTorch (which is installed separately in Dockerfile with CUDA index)

# Core ML packages
pandas>=1.5.0
faster-whisper>=0.10.0
git+https://github.com/m-bain/whisperx.git  # WhisperX from git (as in environment.yml)
pyannote.audio==3.3.2  # Specific version from environment.yml
transformers==4.48.0  # Required by whisperx 3.7.4 (>=4.48.0) and pyannote.audio 3.3.2
torchaudio  # Already installed with PyTorch but explicitly listed

# Utilities
feedparser  # For RSS feed parsing
requests  # For HTTP requests
httpx>=0.25.0  # For async HTTP requests
yt-dlp>=2024.12.0  # For audio downloads (updated for YouTube compatibility)
pydub>=0.25.0  # For audio processing
pydantic  # For data models
python-dotenv  # For environment config
matplotlib  # Required by pyannote.audio/whisperx for visualization

# Database dependencies for PostgreSQL access
asyncpg
sqlalchemy[asyncio]

# Logging and reliability
structlog>=23.0.0  # Structured logging
tenacity>=8.0.0  # Retry logic
