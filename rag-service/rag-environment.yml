name: rag-service
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      # Web Framework
      - fastapi==0.109.0
      - uvicorn[standard]==0.27.0
      - python-multipart==0.0.6  # For file uploads
      
      # Vector Database
      - qdrant-client==1.7.0
      
      # Embeddings
      - sentence-transformers==2.2.2
      - torch==2.1.0
      - transformers==4.36.0
      
      # Gemini API
      - google-generativeai==0.3.2
      
      # File Watching
      - watchdog==3.0.0
      
      # Utilities
      - python-dotenv==1.0.0
      - pandas==2.1.4
      - numpy==1.26.2
      
      # HTTP Client (for CORS)
      - httpx==0.26.0
